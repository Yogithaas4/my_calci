name: CI/CD pipeline

on: 
  workflow_\dispatch: # Only runs manually if you trigger it

jobs:
  build:
    runs-on: ubunlatest
    steps:
      - n\ame: Placeholder step
        run: echo "This is an empty pipeline. Nothing runs on push or PR."
